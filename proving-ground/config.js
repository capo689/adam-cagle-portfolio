window.WORKFLOW_CONFIG={
  title:'Proving Ground',kicker:'Evaluation and model-behavior lab',
  headline:'Prove the change before it ships.',
  intro:'Follow one difficult evaluation item through governed golden-set admission, a versioned rubric, a pinned baseline, independent red-team and judge families, human adjudication, and release gates that can stop a change.',
  stages:['Test item','Calibration','Task routing','System run','Independent judges','Agreement','Adjudication','Regression gate'],
  case:{id:'EV-042',type:'ONE GUIDED EVALUATION',title:'Unsupported consolidation recommendation',summary:'A proposed prompt revision starts making confident merge recommendations when the evidence only supports similarity.',route:'Regression candidate · human adjudication',cardLabel:'Evaluation item',cardSub:'Golden set',cta:'Run the evaluation'},
  nodes:[
    {id:'item',stage:0,lane:-2.6,kind:'data',label:'Golden-set item EV-042',summary:'A frozen input, expected behavior, evidence IDs, and SHA-256 hash make this run exactly reproducible.',decision:'Use the versioned item without modification.',why:'A moving test set can hide regressions by changing the exam.',narration:'We begin with a frozen golden-set case. Its input, expected behavior, and evidence are versioned, so this prompt revision faces the same test as the last one.'},
    {id:'mutate',stage:0,lane:-1.3,kind:'model',tier:'OPENROUTER · SMALL',label:'Adversarial mutation',summary:'A low-cost model creates negation, truncation, and injection variants for later human review.',decision:'Generate candidates, but do not admit them automatically.',why:'Model-generated tests need human labels before they become truth.',narration:'A small routed model can expand the bench cheaply, but generated cases never promote themselves into the golden set.'},
    {id:'rubric',stage:0,lane:0,kind:'data',label:'Versioned rubric registry',summary:'Anchored 0–3 dimensions, examples, hashes, owners, promotion history, and superseded versions define groundedness, authority, completeness, and abstention.',decision:'Pin promoted rubric version seven and its hash to this run.',why:'A score is meaningless without stable criteria, anchor examples, and a traceable promotion decision.',narration:'The rubric registry pins version seven, its examples, hash, owner, and promotion history. A revision can create version eight, but it cannot rewrite this evaluation.'},
    {id:'labels',stage:0,lane:1.3,kind:'human',label:'Human label pair',summary:'Two independent reviewers label mutated candidates and establish the practical agreement ceiling for this task.',decision:'Reconcile the held-out labels and flag ambiguous mutations.',why:'The judge cannot claim more certainty than qualified humans can establish.',narration:'Two humans label the held-out slice and the proposed mutations. Their agreement sets the ceiling the automated judges must clear.'},
    {id:'admission',stage:0,lane:2.6,kind:'gate',label:'GATE · Golden-set admission',summary:'A mutation needs two human labels, evidence, a stable expected behavior, ambiguity review, and an accountable owner before it can enter the frozen set.',decision:'ADMIT one negation case; reject six ambiguous or duplicative mutations.',why:'A model-generated test must never promote itself into the source of truth.',narration:'The admission gate accepts one human-labeled negation case and rejects six weak mutations. Generated tests can propose coverage, but people decide what becomes truth.'},
    {id:'calibrate',stage:1,lane:0,kind:'gate',label:'GATE · Judge calibration',summary:'Judge-to-human Cohen’s kappa must be at least 0.60 on the held-out slice.',decision:'PASS at κ = 0.71.',why:'The rubric and judges clear the published calibration threshold.',narration:'The judge pair reaches point seven one kappa against the human slice. Below point six, the evaluation would stop here and the rubric would go back for repair.'},
    {id:'router',stage:2,lane:0,kind:'code',label:'OpenRouter task selector',summary:'The registry chooses models by evaluation role, quality floor, independence, latency, and price.',decision:'Route the system run normally; assign judges from two different provider families.',why:'Model choice is a policy decision attached to the task, not a scattered product constant.',narration:'OpenRouter now selects by task. The system under test uses the candidate configuration, while the two judges must come from independent provider families.'},
    {id:'baseline',stage:3,lane:-2.1,kind:'model',tier:'PINNED CONTROL',label:'Pinned-baseline replay',summary:'The last-approved prompt, provider, model revision, retrieval snapshot, and tool policy rerun the same frozen item as a control.',decision:'The approved baseline abstains correctly.',why:'A candidate needs a stable control so dataset difficulty is not mistaken for regression.',narration:'The last-approved configuration replays the same item first. It abstains correctly, proving the test still distinguishes the known-good behavior from the candidate.'},
    {id:'redteam',stage:3,lane:-.7,kind:'model',tier:'OPENROUTER · ALT',label:'Red-team attacker',summary:'A third family creates prompt-injection, authority-confusion, and refusal probes at high temperature.',decision:'Run the candidate against the pinned adversarial suite.',why:'A model asked to attack its own family tends to share its blind spots.',narration:'A third family supplies adversarial probes, giving the candidate pressure the authoring family would not reliably apply to itself.'},
    {id:'redgate',stage:3,lane:.7,kind:'gate',label:'GATE · Red-team safety floor',summary:'Pass rate, severity-weighted failures, refusal precision, and injection resistance must clear published release floors.',decision:'PASS at 99.2% with zero high-severity failures.',why:'Averages must not hide a rare but release-blocking safety failure.',narration:'The adversarial suite clears its published pass-rate floor with no high-severity failures. A single severe failure would block the candidate before quality scoring.'},
    {id:'system',stage:3,lane:2.1,kind:'model',tier:'SYSTEM UNDER TEST',label:'Candidate prompt + model',summary:'The proposed production configuration generates a recommendation for the frozen item.',decision:'Recommend merging the two records.',why:'The candidate overweights semantic similarity and underweights authority evidence.',narration:'The candidate system makes the mistake this item was built to expose. It recommends a merge even though the evidence proves only that the pages are similar.'},
    {id:'judges',stage:4,lane:0,kind:'model',tier:'OPENROUTER · 2 FAMILIES',label:'Independent judge pair',summary:'Two blind, temperature-zero judges score the same output and cite the spans behind each score.',decision:'Judge A scores groundedness two; Judge B scores zero.',why:'The two-point split is larger than the allowed per-dimension difference.',narration:'Two independent judges disagree sharply on groundedness. The disagreement is preserved instead of averaged into a reassuring number.'},
    {id:'agreement',stage:5,lane:0,kind:'code',label:'Agreement check',summary:'Deterministic arithmetic compares scores dimension by dimension and tracks disagreement drift.',decision:'ESCALATE because |A − B| = 2.',why:'The published rule permits a maximum one-point difference.',narration:'Code measures the split. Two points exceeds the threshold, so this item cannot enter the aggregate yet.'},
    {id:'tiebreak',stage:6,lane:-.65,kind:'model',tier:'OPENROUTER · FRONTIER',label:'Bounded tie-break',summary:'Only split items reach a higher-cost reasoning model with extended thinking.',decision:'The merge claim is unsupported.',why:'The cited evidence establishes similarity but not duplicate authority.',narration:'A frontier model is affordable here because it sees only the small fraction of disputed items. It agrees that similarity was mistaken for permission to merge.'},
    {id:'human',stage:6,lane:.8,kind:'human',label:'Human adjudication',summary:'A qualified reviewer resolves the remaining ambiguity and can revise the rubric.',decision:'Label the output as a groundedness failure.',why:'Authority interpretation remains accountable to a human reviewer.',narration:'A human makes the final call, records the rationale, and checks whether the rubric needs a clearer anchor for authority claims.'},
    {id:'aggregate',stage:7,lane:-1.4,kind:'code',label:'Score aggregation',summary:'Resolved scores roll up by dimension and compare against the last approved release.',decision:'Groundedness falls below its release floor.',why:'EV-042 changes class under the candidate prompt.',narration:'With the item resolved, deterministic aggregation shows the candidate prompt has pushed groundedness below its floor.'},
    {id:'regression',stage:7,lane:0,kind:'gate',label:'GATE · Regression',summary:'Any dimension below its floor blocks the candidate configuration by default.',decision:'BLOCK the prompt revision.',why:'The system fails closed and attaches the changed-class examples.',narration:'The regression gate blocks the release. There is no override hidden in a prompt and no automatic downgrade to a weaker standard.'},
    {id:'scorecard',stage:7,lane:1.35,kind:'output',label:'Evaluation scorecard',summary:'The artifact shows calibration, disagreement, changed items, red-team results, and the blocked decision.',decision:'Publish the failed evaluation result internally.',why:'A visible failure is evidence that the control system works.',narration:'The final scorecard preserves the failed candidate, the disputed item, and the human rationale. The output is a decision record, not a flattering benchmark.'}
  ],
  edges:[['item','mutate'],['mutate','rubric'],['mutate','item','feedback'],['rubric','labels'],['rubric','calibrate'],['labels','admission'],['admission','calibrate'],['item','calibrate'],['calibrate','router'],['router','baseline'],['router','system'],['baseline','redteam'],['redteam','redgate'],['redteam','system','risk'],['redgate','system'],['system','judges'],['judges','agreement'],['agreement','tiebreak','risk'],['tiebreak','human','risk'],['human','aggregate'],['aggregate','regression'],['regression','scorecard']],
  route:['item','mutate','rubric','labels','admission','calibrate','router','baseline','redteam','redgate','system','judges','agreement','tiebreak','human','aggregate','regression','scorecard'],
  completion:{title:'The release is blocked.',copy:'A governed test set, versioned rubric, pinned control, and red-team floor establish a trustworthy evaluation before the system catches a confident but unsupported merge recommendation and prevents the candidate prompt from shipping.',proof:'Baseline + gated regression',human:'Admission + adjudication',write:'Zero production changes'}
};
