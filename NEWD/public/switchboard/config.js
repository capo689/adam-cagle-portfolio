window.WORKFLOW_CONFIG={
  title:'Switchboard',kicker:'Task-based model routing and cost governance',
  headline:'The right model for every request, with the receipt attached.',
  intro:'A production routing platform that selects models by task policy rather than habit, enforces privacy and residency before transport, validates every result on observable evidence, bounds repair, and attributes cost to the tenth of a cent.',
  stages:['Classify','Policy route','Privacy','Semantic cache','Cheap attempt','Evidence check','Repair + escalation','Result + telemetry'],
  case:{id:'SB-882',type:'ONE GUIDED AI REQUEST',title:'Support-case resolution summary',summary:'A support application requests a cited JSON resolution summary for a customer case. Switchboard classifies the task, routes it to the least expensive qualified model, enforces privacy, validates the result, completes one bounded repair, and returns a valid, cited, fully attributed summary for one point four cents.',route:'Cache miss · bounded repair',cardLabel:'AI request',cardSub:'Support ops',cta:'Route the request'},
  nodes:[
    {id:'request',stage:0,lane:0,kind:'input',label:'Tagged application request',summary:'Input, required schema, citation requirement, tenant, latency budget, and caller identity arrive in one envelope.',decision:'Accept SB-882 with a 2.5-second budget and strict JSON schema.',why:'The caller declares what success means but does not choose a model.',narration:'The request arrives as a single envelope: the input, the required output schema, a citation requirement, the tenant, a latency budget, and the caller\'s identity. The application specifies requirements, not a model.'},
    {id:'classify',stage:0,lane:1.15,kind:'code',label:'Task taxonomy',summary:'Deterministic rules classify the request as extract-and-summarize with grounded citations.',decision:'Assign class SUPPORT_GROUNDED_SUMMARY.',why:'Routing by task keeps model choice consistent across every call site.',narration:'Deterministic classification labels the task as a grounded support summary. That label governs every downstream policy decision, independent of which application submitted it.'},
    {id:'registry',stage:1,lane:-1.25,kind:'data',label:'OpenRouter capability registry',summary:'Per model and task: price, p50 and p95 latency, context, schema reliability, residency, family, and quality measured on the organization\'s own evaluation sets.',decision:'Consider four models that fit context and transport policy.',why:'Quality comes from the Proving Ground bench rather than vendor marketing.',narration:'The capability registry combines live price and latency from OpenRouter with quality scores measured on the organization\'s own evaluation sets. Four models qualify for this task class.'},
    {id:'policy',stage:1,lane:1.25,kind:'data',label:'Routing policy',summary:'A declarative file sets quality floor, latency ceiling, spend ceiling, privacy, fallback, and repair rules per task.',decision:'Require quality ≥ 0.82 and schema reliability ≥ 0.97.',why:'A readable policy is safer than model choices scattered through product code.',narration:'The task policy defines the quality floor, the latency budget, and the bounded fallback rules. It is a readable document that any reviewer can audit.'},
    {id:'router',stage:1,lane:0,kind:'code',label:'OpenRouter constrained selector',summary:'Deterministic optimization chooses the lowest-cost eligible model and logs every rejected candidate.',decision:'Choose the small tier for the first attempt.',why:'It is the cheapest model that clears measured quality, latency, and context requirements.',narration:'The selector chooses the least expensive qualified model and records why each alternative was not chosen: one below the quality floor, one over the latency budget. Every route carries its reasoning.'},
    {id:'sensitivity',stage:2,lane:0,kind:'gate',label:'GATE · Sensitivity + residency',summary:'PII, regulated data, tenant rules, and residency constraints are enforced before transport.',decision:'CLEAR for hosted processing after redaction.',why:'A flagged request cannot reach an external provider, regardless of prompt instructions.',narration:'Personal data, residency, and tenant rules are evaluated before transport. The support thread is redacted and cleared for a hosted model.'},
    {id:'local',stage:2,lane:1.35,kind:'model',tier:'LOCAL · NO EGRESS',label:'Local private route',summary:'Sensitive tasks run on-device under a lower published quality and latency expectation.',decision:'BYPASS because this request is cleared for hosted processing.',why:'Privacy is a transport constraint, not a polite request to a hosted model.',narration:'A local, no-egress route is maintained for requests the sensitivity gate flags, with its quality tradeoff documented in the policy. This request does not require it.'},
    {id:'cachegate',stage:3,lane:-1.15,kind:'gate',label:'GATE · Cache eligibility',summary:'Task class, sensitivity, tenant, policy version, source freshness, and reuse risk determine whether a request may consult cached output at all.',decision:'PASS this grounded support summary under a same-tenant 0.94 threshold.',why:'Creative generation, sensitive decisions, and unstable source tasks should never inherit an answer merely because it looks similar.',narration:'The cache eligibility gate confirms this task class may reuse prior output under same-tenant, same-policy, and freshness rules. Creative generation and sensitive decision tasks are ineligible by policy.'},
    {id:'cache',stage:3,lane:.2,kind:'code',label:'Class-scoped semantic cache',summary:'A tight support-summary threshold searches only same-tenant, same-policy, still-valid results.',decision:'MISS at similarity 0.81 below the 0.94 threshold.',why:'Loose cache reuse could attach the wrong resolution to a customer case.',narration:'The semantic cache is queried. The closest match scores point eight one against a reuse threshold of point nine four. The request proceeds to inference.'},
    {id:'cheap',stage:4,lane:0,kind:'model',tier:'OPENROUTER · SMALL',label:'Low-cost first attempt',summary:'The selected model receives retrieved case evidence, a JSON schema, and short citation IDs.',decision:'Return a useful summary with one malformed citation array.',why:'Content quality is acceptable, but the payload cannot be trusted until it validates.',narration:'The selected small model produces a complete, readable summary. Validation finds one citation field that does not conform to the schema. The result is held for evidence review.'},
    {id:'signals',stage:5,lane:0,kind:'code',label:'Evidence-based confidence',summary:'Schema validation, citation coverage, two-sample consistency, and a small independent judge determine accept or escalate.',decision:'ESCALATE because schema validation fails.',why:'No model is asked how confident it feels; observable signals control the route.',narration:'Confidence is computed from observable evidence: schema validity, citation coverage, agreement across samples, and an independent check. The schema violation triggers escalation under policy.'},
    {id:'escalate',stage:6,lane:-1.1,kind:'model',tier:'OPENROUTER · REASONING',label:'Reasoning-tier retry',summary:'The stronger route receives the original task and validation error, not an open-ended invitation to rewrite.',decision:'Repair content structure but preserve one invalid citation identifier.',why:'The bounded retry improves the payload but still does not clear deterministic validation.',narration:'A reasoning-tier model receives the exact validation failure and repairs the structure. Revalidation finds one citation identifier that does not resolve to a known source.'},
    {id:'repairgate',stage:6,lane:0,kind:'gate',label:'GATE · Bounded repair',summary:'At most two same-family attempts, then one alternate-family repair, then fail closed to a human.',decision:'Use the single alternate-family repair.',why:'The system prevents infinite loops, silent degradation, and unbounded spend.',narration:'The repair gate applies the bounded retry policy: one further attempt, from a different model family, before the request routes to a person.'},
    {id:'repair',stage:6,lane:1.05,kind:'model',tier:'OPENROUTER · ALT FAMILY',label:'Model-diverse repair',summary:'A different provider family repairs only the invalid fields and must preserve supported content.',decision:'Return a schema-valid payload with known citation IDs.',why:'Provider-family diversity reduces repeated house-style formatting failures.',narration:'An alternate model family receives the unresolved citation and resolves it against the source set. The output is submitted to the same validator as every prior attempt.'},
    {id:'revalidate',stage:6,lane:1.8,kind:'code',label:'Deterministic revalidation',summary:'The repaired payload faces the original schema, citation allowlist, and support checks with no relaxed standard.',decision:'PASS the repaired payload.',why:'Repair only matters if the result clears the same contract that rejected the first attempt.',narration:'The original validator reruns with no rule relaxed. Every citation resolves and every required field is present. The result is valid.'},
    {id:'human',stage:6,lane:2.15,kind:'human',label:'Human fail-closed terminus',summary:'If the alternate-family payload fails, the request stops with its evidence, errors, and cost attached.',decision:'BYPASS because the repaired payload passes.',why:'The unhappy path ends with accountable review, never a degraded automatic answer.',narration:'A human review queue is the defined endpoint for any request that exhausts bounded repair without validating. This request validated and does not require it.'},
    {id:'result',stage:7,lane:-1.2,kind:'output',label:'Validated attributed result',summary:'The caller receives the schema-valid answer plus model, tier, route, citations, latency, and cost.',decision:'Return the repaired summary to the support application.',why:'Every production answer needs provenance sufficient for debugging and review.',narration:'The application receives a schema-valid, fully cited resolution summary, with the complete route that produced it attached.'},
    {id:'telemetry',stage:7,lane:0,kind:'code',label:'OTel span + cost ledger',summary:'GenAI semantic-convention spans record tokens, cache status, attempts, models, latency, errors, and attributed cost.',decision:'Record three model calls costing $0.014 total.',why:'Savings and escalation claims should be reconstructable from standard telemetry.',narration:'Every attempt is recorded as an observability span with its cost: three model calls, one repair event, one point four cents in total. Routing savings and cache savings are attributed separately.'},
    {id:'budget',stage:7,lane:1.1,kind:'gate',label:'GATE · Spend anomaly',summary:'Rolling task baselines detect abnormal spend and pin the router to a last-known-good policy until a human unpins it.',decision:'PASS inside the support-summary budget band.',why:'Cost governance needs a kill switch, not merely a chart.',narration:'The budget gate compares this route\'s cost against its rolling baseline and finds it within range. The policy remains active.'},
    {id:'dashboard',stage:7,lane:2.15,kind:'output',label:'Cost-quality frontier',summary:'Quality, cost, latency, cache hit rate, escalation, repair, and failure traces are explorable by task class.',decision:'Update the grounded-summary frontier and repair rate.',why:'A task-level view shows whether cheap-first routing is actually working.',narration:'The cost-quality frontier for this task class updates with the complete request, repair path included. One request, the lowest qualified cost, every decision on record. Multiply that by every request an organization makes, and this is what governed AI spend looks like.'}
  ],
  edges:[['request','classify'],['classify','registry'],['registry','policy'],['registry','router'],['policy','router'],['router','sensitivity'],['sensitivity','local','risk'],['local','cachegate','feedback'],['sensitivity','cachegate'],['cachegate','cache'],['cache','cheap'],['cheap','signals'],['signals','result'],['signals','escalate','risk'],['escalate','repairgate'],['repairgate','repair','risk'],['repair','revalidate'],['revalidate','human','risk'],['human','result','feedback'],['revalidate','result'],['repair','repairgate','feedback'],['repairgate','human','risk'],['repairgate','result'],['result','telemetry'],['telemetry','budget'],['budget','dashboard'],['budget','router','feedback']],
  route:['request','classify','registry','policy','router','sensitivity','local','cachegate','cache','cheap','signals','escalate','repairgate','repair','revalidate','human','result','telemetry','budget','dashboard'],
  completion:{title:'Valid, cited, attributed, and costed.',copy:'The application receives a schema-valid, fully cited resolution summary with its complete route attached. Telemetry records three model calls, one bounded repair, and a total cost of one point four cents, and the task\'s cost-quality frontier is updated with the full path included.',proof:'Deterministic validation on every attempt',human:'Fail-closed terminus for unresolved requests',write:'Validated result and cost ledger'}
};
