window.WORKFLOW_CONFIG={
  title:'Switchboard',kicker:'Task-based model router and cost observatory',
  headline:'Spend intelligence where the task earns it.',
  intro:'Follow one structured AI request through task classification, privacy enforcement, semantic caching, evidence-based escalation, model-diverse repair, and full cost attribution.',
  stages:['Classify','Policy route','Privacy','Semantic cache','Cheap attempt','Evidence check','Repair + escalation','Result + telemetry'],
  case:{id:'SB-882',type:'ONE GUIDED AI REQUEST',title:'Support-case resolution summary',summary:'A long support thread needs a cited JSON summary. It is non-sensitive, misses cache, and the first low-cost response fails its schema.',route:'Cache miss · bounded repair',cardLabel:'AI request',cardSub:'Support ops',cta:'Route the request'},
  nodes:[
    {id:'request',stage:0,lane:0,kind:'input',label:'Tagged application request',summary:'Input, required schema, citation requirement, tenant, latency budget, and caller identity arrive in one envelope.',decision:'Accept SB-882 with a 2.5-second budget and strict JSON schema.',why:'The caller declares what success means but does not choose a model.',narration:'A support application asks for a cited resolution summary. It supplies the schema, tenant, and latency budget, but it cannot hard-code which model runs.'},
    {id:'classify',stage:0,lane:1.15,kind:'code',label:'Task taxonomy',summary:'Deterministic rules classify the request as extract-and-summarize with grounded citations.',decision:'Assign class SUPPORT_GROUNDED_SUMMARY.',why:'Routing by task keeps model choice consistent across every call site.',narration:'The request is classified as a grounded support summary. That task label—not the application name—drives every downstream policy.'},
    {id:'registry',stage:1,lane:-1.25,kind:'data',label:'OpenRouter capability registry',summary:'Per model and task: measured quality, price, p50 and p95 latency, context, schema reliability, residency, and family.',decision:'Consider four models that fit context and transport policy.',why:'Quality comes from the Proving Ground bench rather than vendor marketing.',narration:'The OpenRouter registry combines live price and latency with quality measured on our own golden sets. Four candidates fit this request.'},
    {id:'policy',stage:1,lane:1.25,kind:'data',label:'Routing policy',summary:'A declarative file sets quality floor, latency ceiling, spend ceiling, privacy, fallback, and repair rules per task.',decision:'Require quality ≥ 0.82 and schema reliability ≥ 0.97.',why:'A readable policy is safer than model choices scattered through product code.',narration:'The task policy establishes a quality floor, a latency budget, and bounded fallback rules that any reviewer can inspect.'},
    {id:'router',stage:1,lane:0,kind:'code',label:'OpenRouter constrained selector',summary:'Deterministic optimization chooses the lowest-cost eligible model and logs every rejected candidate.',decision:'Choose the small tier for the first attempt.',why:'It is the cheapest model that clears measured quality, latency, and context requirements.',narration:'The router chooses the least expensive eligible model and records why the cheaper and slower alternatives were rejected.'},
    {id:'sensitivity',stage:2,lane:0,kind:'gate',label:'GATE · Sensitivity + residency',summary:'PII, regulated data, tenant rules, and residency constraints are enforced before transport.',decision:'CLEAR for hosted processing after redaction.',why:'A flagged request cannot reach an external provider, regardless of prompt instructions.',narration:'The sensitivity gate confirms the redacted thread can use a hosted model. If it were flagged, transport would force a local route before any external call.'},
    {id:'local',stage:2,lane:1.35,kind:'model',tier:'LOCAL · NO EGRESS',label:'Local private route',summary:'Sensitive tasks run on-device under a lower published quality and latency expectation.',decision:'BYPASS because this request is cleared for hosted processing.',why:'Privacy is a transport constraint, not a polite request to a hosted model.',narration:'The tour pauses at the private branch the sensitivity gate would use for flagged work. This request bypasses it, but the local route and its quality tradeoff remain explicit.'},
    {id:'cachegate',stage:3,lane:-1.15,kind:'gate',label:'GATE · Cache eligibility',summary:'Task class, sensitivity, tenant, policy version, source freshness, and reuse risk determine whether a request may consult cached output at all.',decision:'PASS this grounded support summary under a same-tenant 0.94 threshold.',why:'Creative generation, sensitive decisions, and unstable source tasks should never inherit an answer merely because it looks similar.',narration:'The cache gate confirms that this grounded support task is eligible under the same tenant, policy, and freshness window. Creative and sensitive decision tasks would bypass reuse entirely.'},
    {id:'cache',stage:3,lane:.2,kind:'code',label:'Class-scoped semantic cache',summary:'A tight support-summary threshold searches only same-tenant, same-policy, still-valid results.',decision:'MISS at similarity 0.81 below the 0.94 threshold.',why:'Loose cache reuse could attach the wrong resolution to a customer case.',narration:'The eligible request now checks the semantic cache. Similarity reaches point eight one, below the point nine four threshold, so it proceeds to inference.'},
    {id:'cheap',stage:4,lane:0,kind:'model',tier:'OPENROUTER · SMALL',label:'Low-cost first attempt',summary:'The selected model receives retrieved case evidence, a JSON schema, and short citation IDs.',decision:'Return a useful summary with one malformed citation array.',why:'Content quality is acceptable, but the payload cannot be trusted until it validates.',narration:'The small model produces a good summary, but one citation field violates the schema. The system does not confuse readable text with a valid result.'},
    {id:'signals',stage:5,lane:0,kind:'code',label:'Evidence-based confidence',summary:'Schema validation, citation coverage, two-sample consistency, and a small independent judge determine accept or escalate.',decision:'ESCALATE because schema validation fails.',why:'No model is asked how confident it feels; observable signals control the route.',narration:'Confidence comes from evidence: schema validity, citation coverage, sample agreement, and an independent check. The failed schema triggers escalation.'},
    {id:'escalate',stage:6,lane:-1.1,kind:'model',tier:'OPENROUTER · REASONING',label:'Reasoning-tier retry',summary:'The stronger route receives the original task and validation error, not an open-ended invitation to rewrite.',decision:'Repair content structure but preserve one invalid citation identifier.',why:'The bounded retry improves the payload but still does not clear deterministic validation.',narration:'A reasoning model receives the exact validation failure. It repairs the structure, but one citation identifier is still unknown.'},
    {id:'repairgate',stage:6,lane:0,kind:'gate',label:'GATE · Bounded repair',summary:'At most two same-family attempts, then one alternate-family repair, then fail closed to a human.',decision:'Use the single alternate-family repair.',why:'The system prevents infinite loops, silent degradation, and unbounded spend.',narration:'The repair gate refuses another vague retry. Policy allows one model-diverse repair before a human handoff.'},
    {id:'repair',stage:6,lane:1.05,kind:'model',tier:'OPENROUTER · ALT FAMILY',label:'Model-diverse repair',summary:'A different provider family repairs only the invalid fields and must preserve supported content.',decision:'Return a schema-valid payload with known citation IDs.',why:'Provider-family diversity reduces repeated house-style formatting failures.',narration:'A different model family repairs the malformed fields. The output then faces the same deterministic validator as every other attempt.'},
    {id:'revalidate',stage:6,lane:1.8,kind:'code',label:'Deterministic revalidation',summary:'The repaired payload faces the original schema, citation allowlist, and support checks with no relaxed standard.',decision:'PASS the repaired payload.',why:'Repair only matters if the result clears the same contract that rejected the first attempt.',narration:'The alternate-family response receives no special treatment. The original validator reruns and confirms every citation and required field.'},
    {id:'human',stage:6,lane:2.15,kind:'human',label:'Human fail-closed terminus',summary:'If the alternate-family payload fails, the request stops with its evidence, errors, and cost attached.',decision:'BYPASS because the repaired payload passes.',why:'The unhappy path ends with accountable review, never a degraded automatic answer.',narration:'The tour also inspects the human terminus. This successful repair bypasses it, but any final validation failure must stop here rather than return a degraded automatic answer.'},
    {id:'result',stage:7,lane:-1.2,kind:'output',label:'Validated attributed result',summary:'The caller receives the schema-valid answer plus model, tier, route, citations, latency, and cost.',decision:'Return the repaired summary to the support application.',why:'Every production answer needs provenance sufficient for debugging and review.',narration:'The application receives a valid, cited summary together with the complete route that produced it.'},
    {id:'telemetry',stage:7,lane:0,kind:'code',label:'OTel span + cost ledger',summary:'GenAI semantic-convention spans record tokens, cache status, attempts, models, latency, errors, and attributed cost.',decision:'Record three model calls costing $0.014 total.',why:'Savings and escalation claims should be reconstructable from standard telemetry.',narration:'Every attempt becomes an observability span with cost attached. The dashboard can distinguish routing savings from cache savings without double counting.'},
    {id:'budget',stage:7,lane:1.1,kind:'gate',label:'GATE · Spend anomaly',summary:'Rolling task baselines detect abnormal spend and pin the router to a last-known-good policy until a human unpins it.',decision:'PASS inside the support-summary budget band.',why:'Cost governance needs a kill switch, not merely a chart.',narration:'The budget gate compares this route with its rolling baseline. A real anomaly would pin the last safe policy and require a human to restore normal routing.'},
    {id:'dashboard',stage:7,lane:2.15,kind:'output',label:'Cost-quality frontier',summary:'Quality, cost, latency, cache hit rate, escalation, repair, and failure traces are explorable by task class.',decision:'Update the grounded-summary frontier and repair rate.',why:'A task-level view shows whether cheap-first routing is actually working.',narration:'The final dashboard updates the cost-quality frontier for this task class, including the repair path instead of hiding it inside an average.'}
  ],
  edges:[['request','classify'],['classify','registry'],['registry','policy'],['registry','router'],['policy','router'],['router','sensitivity'],['sensitivity','local','risk'],['local','cachegate','feedback'],['sensitivity','cachegate'],['cachegate','cache'],['cache','cheap'],['cheap','signals'],['signals','result'],['signals','escalate','risk'],['escalate','repairgate'],['repairgate','repair','risk'],['repair','revalidate'],['revalidate','human','risk'],['human','result','feedback'],['revalidate','result'],['repair','repairgate','feedback'],['repairgate','human','risk'],['repairgate','result'],['result','telemetry'],['telemetry','budget'],['budget','dashboard'],['budget','router','feedback']],
  route:['request','classify','registry','policy','router','sensitivity','local','cachegate','cache','cheap','signals','escalate','repairgate','repair','revalidate','human','result','telemetry','budget','dashboard'],
  completion:{title:'The request is valid, attributable, and costed.',copy:'Switchboard verifies that reuse is permitted before touching the cache, starts cheap, escalates only on observable failure, uses one alternate-family repair, returns a cited schema-valid result, and accounts for every model call.',proof:'Eligible + validated',human:'Fail-closed fallback',write:'Result returned to caller'}
};
