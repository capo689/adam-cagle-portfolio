window.WORKFLOW_CONFIG={
  title:'Proving Ground',kicker:'Evaluation and release governance for AI systems',
  headline:'Measure the change. Then decide.',
  intro:'A governed evaluation system that tests every proposed change to a production AI configuration against a versioned test set, calibrated judges, a pinned baseline, and explicit release standards, and delivers a release decision backed by evidence.',
  stages:['Test item','Calibration','Task routing','System run','Independent judges','Agreement','Adjudication','Regression gate'],
  case:{id:'EV-042',type:'ONE GUIDED EVALUATION',title:'Prompt revision release review',summary:'A proposed prompt revision for a records-consolidation assistant is evaluated for release. The review confirms safety, identifies a groundedness regression, and delivers a documented release decision with the exact case and rationale the engineering team needs to correct it.',route:'Regression candidate · human adjudication',cardLabel:'Evaluation item',cardSub:'Golden set',cta:'Run the evaluation'},
  nodes:[
    {id:'item',stage:0,lane:-2.6,kind:'data',label:'Golden-set item EV-042',summary:'A frozen input, expected behavior, evidence IDs, and SHA-256 hash make this run exactly reproducible.',decision:'Use the versioned item without modification.',why:'A moving test set can hide regressions by changing the exam.',narration:'The review begins with a governed test case. Its input, expected behavior, and supporting evidence are versioned and hashed, so every candidate configuration is measured against an identical standard.'},
    {id:'mutate',stage:0,lane:-1.3,kind:'model',tier:'OPENROUTER · SMALL',label:'Adversarial mutation',summary:'A low-cost model creates negation, truncation, and injection variants for later human review.',decision:'Generate candidates, but do not admit them automatically.',why:'Model-generated tests need human labels before they become truth.',narration:'A small routed model proposes variations on existing cases: negations, unit changes, truncations. It expands coverage at low cost. Every proposal enters a review queue; none enters the test set on its own.'},
    {id:'rubric',stage:0,lane:0,kind:'data',label:'Versioned rubric registry',summary:'Anchored 0–3 dimensions, examples, hashes, owners, promotion history, and superseded versions define groundedness, authority, completeness, and abstention.',decision:'Pin promoted rubric version seven and its hash to this run.',why:'A score is meaningless without stable criteria, anchor examples, and a traceable promotion decision.',narration:'The scoring rubric is a versioned artifact with an owner, anchor examples at every score level, a hash, and a promotion history. This review is bound to version seven. A future revision creates version eight without altering the standard applied here.'},
    {id:'labels',stage:0,lane:1.3,kind:'human',label:'Human label pair',summary:'Two independent reviewers label mutated candidates and establish the practical agreement ceiling for this task.',decision:'Reconcile the held-out labels and flag ambiguous mutations.',why:'The judge cannot claim more certainty than qualified humans can establish.',narration:'Two human raters label the held-out slice and the proposed mutations independently. Their agreement with each other sets the ceiling for automated judging.'},
    {id:'admission',stage:0,lane:2.6,kind:'gate',label:'GATE · Golden-set admission',summary:'A mutation needs two human labels, evidence, a stable expected behavior, ambiguity review, and an accountable owner before it can enter the frozen set.',decision:'ADMIT one negation case; reject six ambiguous or duplicative mutations.',why:'A model-generated test must never promote itself into the source of truth.',narration:'The admission gate compares each proposed case against the rubric and both human labels. One negation case meets the standard and is admitted. Six are declined as ambiguous or duplicative. The test set grows only through this gate.'},
    {id:'calibrate',stage:1,lane:0,kind:'gate',label:'GATE · Judge calibration',summary:'Judge-to-human Cohen’s kappa must be at least 0.60 on the held-out slice.',decision:'PASS at κ = 0.71.',why:'The rubric and judges clear the published calibration threshold.',narration:'Before scoring, the judge pair is calibrated against the human-labeled slice. Agreement is point seven one kappa against a published floor of point six. The judges are qualified to score this review.'},
    {id:'router',stage:2,lane:0,kind:'code',label:'OpenRouter task selector',summary:'The registry chooses models by evaluation role, quality floor, independence, latency, and price.',decision:'Route the system run normally; assign judges from two different provider families.',why:'Model choice is a policy decision attached to the task, not a scattered product constant.',narration:'Model selection is by role. The candidate runs in its own proposed configuration, because that configuration is what is under review. The two judges are drawn from separate provider families, and neither shares a family with the candidate.'},
    {id:'baseline',stage:3,lane:-2.1,kind:'model',tier:'PINNED CONTROL',label:'Pinned-baseline replay',summary:'The last-approved prompt, provider, model revision, retrieval snapshot, and tool policy rerun the same frozen item as a control.',decision:'The approved baseline abstains correctly.',why:'A candidate needs a stable control so dataset difficulty is not mistaken for regression.',narration:'The current production configuration runs the case first and abstains, which is the expected behavior. That result is the control. It confirms the test distinguishes correct behavior before the candidate is measured.'},
    {id:'redteam',stage:3,lane:-.7,kind:'model',tier:'OPENROUTER · ALT',label:'Red-team attacker',summary:'A third family creates prompt-injection, authority-confusion, and refusal probes at high temperature.',decision:'Generate the pinned adversarial suite for this candidate.',why:'A model asked to attack its own family tends to share its blind spots.',narration:'A third model family generates the adversarial suite: injected instructions, brand-safety probes, extraction attempts, and refusal traps. Independence from the candidate\'s family is a requirement, not a preference.'},
    {id:'system',stage:3,lane:.7,kind:'model',tier:'SYSTEM UNDER TEST',label:'Candidate prompt + model',summary:'The proposed production configuration answers the frozen evaluation item and the pinned adversarial suite.',decision:'Recommend merging the two records and return responses for every adversarial probe.',why:'The candidate overweights semantic similarity and underweights authority evidence on the golden item.',narration:'The candidate answers the golden case and the full adversarial suite. It handles every safety probe correctly. On the golden case, it recommends consolidating two records whose evidence establishes similarity but not identity. That response is recorded for scoring.'},
    {id:'redgate',stage:3,lane:2.1,kind:'gate',label:'GATE · Red-team safety floor',summary:'The candidate’s pass rate, severity-weighted failures, refusal precision, and injection resistance must clear published release floors.',decision:'PASS at 99.2% with zero high-severity failures.',why:'Averages must not hide a rare but release-blocking safety failure.',narration:'The safety gate measures the adversarial responses against the published floor. No high-severity findings. The candidate is cleared for quality scoring.'},
    {id:'judges',stage:4,lane:0,kind:'model',tier:'OPENROUTER · 2 FAMILIES',label:'Independent judge pair',summary:'Two blind, temperature-zero judges score the same output and cite the spans behind each score.',decision:'Judge A scores groundedness two; Judge B scores zero.',why:'The two-point split is larger than the allowed per-dimension difference.',narration:'Both judges score the golden case against rubric version seven. On groundedness they disagree: one scores a two, the other a zero. The system records both scores rather than averaging them.'},
    {id:'agreement',stage:5,lane:0,kind:'code',label:'Agreement check',summary:'Deterministic arithmetic compares scores dimension by dimension and tracks disagreement drift.',decision:'ESCALATE because |A − B| = 2.',why:'The published rule permits a maximum one-point difference.',narration:'Deterministic code measures the split at two points against a published limit of one. The item is routed to adjudication before it can enter the aggregate.'},
    {id:'tiebreak',stage:6,lane:-.65,kind:'model',tier:'OPENROUTER · FRONTIER',label:'Bounded tie-break',summary:'Only split items reach a higher-cost reasoning model with extended thinking.',decision:'The merge claim is unsupported.',why:'The cited evidence establishes similarity but not duplicate authority.',narration:'A frontier model reviews the disputed item and provides an independent third read: the evidence supports similarity, not consolidation. Because it sees only disputed items, this stage stays affordable at scale.'},
    {id:'human',stage:6,lane:.8,kind:'human',label:'Human adjudication',summary:'A qualified reviewer resolves the remaining ambiguity and can revise the rubric.',decision:'Label the output as a groundedness failure.',why:'Authority interpretation remains accountable to a human reviewer.',narration:'A reviewer makes the final determination, assigns the groundedness score, and records the rationale. The reviewer also files a rubric revision request to add a clearer anchor for authority claims, so the next judge pair scores this pattern consistently.'},
    {id:'aggregate',stage:7,lane:-1.4,kind:'code',label:'Score aggregation',summary:'Resolved scores roll up by dimension and compare against the last approved release.',decision:'Groundedness falls below its release floor.',why:'EV-042 changes class under the candidate prompt.',narration:'With the item resolved, deterministic aggregation produces the candidate\'s scorecard. Safety is at standard. Groundedness is below its release floor.'},
    {id:'regression',stage:7,lane:0,kind:'gate',label:'GATE · Regression',summary:'Any dimension below its floor blocks the candidate configuration by default.',decision:'BLOCK the prompt revision.',why:'The system fails closed and attaches the changed-class examples.',narration:'The regression gate holds the release. The decision is recorded with the failing item, the scores, and the adjudication rationale attached. There is no override in the prompt layer and no automatic fallback to a weaker standard.'},
    {id:'scorecard',stage:7,lane:1.35,kind:'output',label:'Evaluation scorecard',summary:'The artifact shows calibration, disagreement, changed items, red-team results, and the blocked decision.',decision:'Deliver the documented release decision to the engineering team.',why:'The team receives the exact evidence required to correct the candidate and resubmit it.',narration:'The evaluation record is delivered to the engineering team: the release decision, the reproducible case, the baseline control, both judge scores, the human rationale, and the rubric follow-up. The team knows exactly what to fix and exactly how it will be measured. That is what it means to ship AI on evidence.'}
  ],
  edges:[['item','mutate'],['mutate','rubric'],['mutate','item','feedback'],['rubric','labels'],['rubric','calibrate'],['labels','admission'],['admission','calibrate'],['item','calibrate'],['calibrate','router'],['router','baseline'],['router','system'],['baseline','redteam'],['redteam','system','risk'],['system','redgate'],['system','judges'],['redgate','judges'],['judges','agreement'],['agreement','tiebreak','risk'],['tiebreak','human','risk'],['human','aggregate'],['aggregate','regression'],['regression','scorecard']],
  route:['item','mutate','rubric','labels','admission','calibrate','router','baseline','redteam','system','redgate','judges','agreement','tiebreak','human','aggregate','regression','scorecard'],
  completion:{title:'A release decision, fully documented.',copy:'The candidate cleared the safety floor and fell below the groundedness floor on one adjudicated case. The evaluation record delivers the decision, the reproducible failing item, the judge and human rationale, and the rubric follow-up, so the engineering team can correct the revision against a known case and resubmit it for evaluation with evidence instead of opinion.',proof:'Pinned baseline plus gated regression',human:'Golden-set admission and final adjudication',write:'Evaluation record and release decision'}
};
